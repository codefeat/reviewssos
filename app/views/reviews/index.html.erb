<p id="notice"><%= notice %></p>
<div class="hero-unit" style="background: #eeeeee;
    padding: 60px;
    border-radius: 6px;">
  <h1>Heading</h1>
  <p>Tagline</p>
  <p>
    <a class="btn btn-primary btn-large">
      Learn more
    </a>
  </p>
</div>
<h1>Reviews</h1>
<%= 
#url = ARGV[0] || (puts "https://www.yelp.com/biz/egg-house-cafe-crystal"; gets.strip)

url = 'https://www.yelp.com/biz/egg-house-cafe-crystal'

page = MetaInspector.new('https://www.yelp.com/biz/egg-house-cafe-crystal') 

page.parsed

doc = page.parsed

review = doc.css("div.biz-page-actions a") [0]['href']


link_to "write a review", "https://yelp.com#{review}", class: "btn-primary btn", style: "color:white;"

%><br />

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Picture</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reviews.each do |review| %>
      <tr>
        <td><%= review.name %></td>
        <td><%= review.description %></td>
        <td><%= review.picture %></td>
        <td><%= link_to 'Show', review %></td>
        <td><%= link_to 'Edit', edit_review_path(review) %></td>
        <td><%= link_to 'Destroy', review, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Review', new_review_path %>
